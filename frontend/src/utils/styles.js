import {Colors} from '../utils/consts';

export const Table001 = {
    Headers : {    
      backgroundColor: Colors.Secondary.Main,
      color: '#fff',
      fontSize: 'large'
    }
};

export const Table001_s = {
  Headers : {    
    backgroundColor: Colors.Secondary.Main,
    color: '#fff',
    fontSize: 'small'
  }
};